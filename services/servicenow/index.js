/**
 * ServiceNow — Connector
 *
 * Always uses the live ServiceNow REST API.
 * Stub removed — dev instance is the standard.
 *
 * Required in .env:
 *   SERVICENOW_INSTANCE, SERVICENOW_USERNAME, SERVICENOW_PASSWORD
 */

module.exports = require("./real");
